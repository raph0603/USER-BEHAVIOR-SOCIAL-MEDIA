name: CI

on:
  pull_request:
    branches:
      - main
      - production
  push:
    branches:
      - main
      - production
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Validation
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder validation
        run: |
          echo "No tests configured yet."
          echo "This placeholder keeps the release workflow ready."
