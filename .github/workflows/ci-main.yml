name: Main Branch Validation

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  main-validation:
    name: main-validation
    runs-on: ubuntu-latest

    steps:
      - name: Placeholder validation
        run: |
          echo "No tests configured yet."
          echo "This placeholder validates main before production promotion."
